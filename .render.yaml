services:
  - type: web
    name: gender-norms-predictor
    runtime: python
    buildCommand: "pip install uv && uv sync"
    startCommand: "uv run streamlit run app.py --server.port 10000 --server.address 0.0.0.0"
